<%- include('./partials/head') %>

<main>
    <section class="top-content">
        <h1>Neem een 👀 kijkje naar mijn 💻 werk.</h1>
    </section>

    <% portfolioItems.forEach(item => { %>
        <section class="<%= item.BGColor %>">
            <p><%= item.title %></p>
        </section>
    <% }); %>
</main>

<%- include('./partials/foot') %>